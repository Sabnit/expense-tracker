const iconItems = {
  closeIconAddTransactionForm: document?.getElementById(
    "close-icon"
  ) as HTMLElement,
  closeIconEditTransactionItem: document?.getElementById(
    "close-transaction-icon"
  ) as HTMLElement,
};

export default iconItems;
